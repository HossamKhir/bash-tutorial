#! /usr/bin/bash

# a wildcard is the '*' used in place of a string, or a substring to include whatever
# sequence that might apply to the argument

# let's copy all script files

# cp *.sh ./src/
# whatever the name of the file might be, as long as the extension is ".sh", it will be copied